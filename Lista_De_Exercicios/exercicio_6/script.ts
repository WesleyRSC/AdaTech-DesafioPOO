// 6. Crie uma classe genérica chamada FormaGeometrica onde, a mesma
// deverá ter um atributo chamado nomeDaForma (no caso desta classe
// em específica, o atributo deverá ser inicializado com o valor padrão
// "genérica". Além disso, precisaremos de um método chamado
// obterInformacao, ao qual deverá logar a informação "forma geométrica
// genérica". A partir disso, crie mais três classes que herdem dessa
// classe FormaGeométrica (um círculo, um quadrado e um triangulo).
// Cada forma geométrica deverá implementar o mesmo método
// "obterInformação" porém, trazendo a informação sobre aquela
// determinada forma geométrica (ou seja, no caso do círculo, quando o
// método obterInformacao for chamado, deverá trazer o log "forma
// geométrica círculo", e assim sucessivamente);